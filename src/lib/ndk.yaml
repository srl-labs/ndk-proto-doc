services:
  sdk:
    description: Main interface between custom apps and NDK
    name: SDK
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/sdk_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  telemetry:
    description: A service to manage application's state
    name: Telemetry
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/telemetry_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  appid:
    description: Application identification between router agents and SDK service manager
    name: App ID
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/appid_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  bfd:
    description: API with BFD Manager
    name: BFD
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/bfd_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  config:
    description: Receiving configuration from the SR Linux Management Server
    name: Config
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/config_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  interface:
    description: Receiving interface events
    name: Interface
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/interface_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  lldp:
    description: Receiving LLDP events
    name: LLDP
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/lldp_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  mpls:
    description: Receiving MPLS events
    name: MPLS
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/mpls_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  networkinstance:
    description: Receiving network instance events
    name: Network Instance
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/networkinstance_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  nexthop_group:
    description: Receiving next-hop group events
    name: Next-hop Group
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/nexthop_group_service.proto
      # v0.1.1:
      #   documentation:
      #   source:

  route:
    description: Receiving and setting routes
    name: Route
    versions:
      v0.2.0:
        documentation:
        source: https://github.com/nokia/srlinux-ndk-protobufs/blob/v0.2.0/ndk/route_service.proto
      # v0.1.1:
      #   documentation:
      #   source:
